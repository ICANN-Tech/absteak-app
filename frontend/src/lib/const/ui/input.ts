// Input and Form UI Constants
// Konstanta untuk styling input, label, icon, dan pesan
// Menggunakan centralized variants dari base.ts

import { UI_COMPONENT_VARIANTS } from './base';

export const UI_LABELS = UI_COMPONENT_VARIANTS.labels;
export const UI_ICONS = UI_COMPONENT_VARIANTS.icons;
export const UI_MESSAGES = UI_COMPONENT_VARIANTS.messages;